# 赛车原型优化记录 · 2026-09-04

## 本次目标

- 本轮按反馈将赛道世界比例从 4.15 缩回 2.075（上一版的一半），同时把远景、雾效、地面和阴影范围同步收紧，避免地图过大导致场景稀薄。
- 车辆换回上一版的 Kenney Car Kit `sedan-sports.glb`；用户提供的 Arcade GLB 保留在公共资源中，不删除。
- 移除 64px 道路图块的直接铺贴（该图块是带边框的道路切片，拉伸后会糊且出现绿色边缘），改为 256px 程序化柏油微表面；草地也使用 512px 细节纹理。
- 设施净空改为模型最大边长的保守半径，并增加放置重试间隔；道路实体保持净空，地面模型不会贴进赛道或相互紧贴。
- 赛道有效路宽调整为 16.2 world units，并同步路肩、刹车牌、设施安全净空；中心线与路缘仍保持路面净空，不放置实体模型。
- 4 条路线改为高密度 Catmull-Rom 中心线：Monza 的 Rettifilo/Lesmo/Ascari/Parabolica，Spa 的 La Source/Eau Rouge/Kemmel/Pouhon/Bus Stop，Silverstone 的 Abbey/Village/Maggotts-Becketts/Hangar/Stowe/Club，Nürburgring GP 的 Mercedes Arena/Schumacher S/Dunlop/Valvoline/Bit-Kurve/Coca-Cola。
- 赛道采样保持 1040、弧长细分 2600；世界比例按本轮反馈最终为 2.075，地面、雾效、摄像机远裁剪和远景山体与缩小后的范围同步。
- 移除引擎盖视角，只保留远景追车与近景追车；`C` 在两种视角间切换。
- 自动挡改为以预测 RPM 为主、速度为安全门槛的带迟滞变速箱：1–6 档使用传动比，约 7650 RPM 升挡、3150 RPM 降挡，并有换挡锁止。
- 引擎声改为 Web Audio 多层合成：点火脉冲、发动机主体、谐波、进气、排气、机械噪声、风噪/胎噪和换挡瞬态。
- 驾驶模拟改为 60 Hz 固定步进；油门采用渐进输入，不再一按键就瞬间给满牵引力。
- 加速曲线重新标定为渐进的 GT 级动力：1 档约 62 km/h、2 档约 101 km/h、3 档约 142 km/h，最高约 302 km/h；起步固定从 N → 1 档，不再一按油门跳到高档。
- 车辆更新加入纵向速度、侧向抓地和有惯性的 yaw rate；高速自动减小转向角，收油时保留发动机制动，S 键先刹车后倒车，草地降低抓地力。
- 远景/近景都保持第三人称完整车身构图，镜头不再贴着车尾或引擎盖；镜头参数固定，速度感不依赖视觉放大。
- 本轮性能与速度感优化：建图时缓存 1040 个中心线点/切线，最近点与小地图不再每帧重算 Catmull-Rom；路缘与中心虚线合并为 InstancedMesh（由约 520 个独立 Mesh 降为少量实例批次）；DPR 上限调整为 1.5、阴影贴图调整为 1024，并启用高性能 WebGL 提示。
- HUD 数值和小地图改为 30 Hz 刷新，车辆物理与音频仍保持 60 Hz，减少 DOM 布局抖动。
- （已撤回视觉补偿）镜头保持固定第三人称距离、高度和自然 FOV，不再用 FOV、速度线或镜头振动伪造速度感；速度感以真实世界位移标尺为准。
- 速度单位重新标定：由于 `WORLD_SCALE` 已从 4.15 缩为 2.075，当前 `SPEED_TO_KMH = 3.2`，封顶速度为 `302 / 3.2` world units/s；200 km/h 现在对应约 62.5 world units/s 的真实位移。起步加速度调整为 4.8，换挡阈值仍按 km/h 工作。
- 修正设施真实包围盒净空校验：模型最终落点以包围盒中心对齐后再检查，柏油区域禁止任何实体设施；缓存为空时不再进行错误放置。
- 刹车牌、赛段标牌和维修区平台也采用同一条硬净空规则；跨越相邻弯段时若无安全落点则跳过，不会把标牌/设施生成到另一段柏油上。
- 视角与复位：默认近景第三人称；每次开始新比赛强制回到近景，`C` 仍可切换远景。比赛中按一次 `R` 取车辆与缓存中心线的最近点并对齐切线，复位后从该赛道进度继续，而不是回到起点；忽略按住 R 产生的重复 keydown，避免车辆被连续锁定。

## 音频矩阵

| 事件 / 状态 | 实现 | 运行时触发 |
|---|---|---|
| 怠速 / 加速 | 连续 RPM 点火脉冲 + 发动机主体层 | 每帧由 `engineRpm` 驱动 |
| 自动升挡 | 三角换挡提示 + 排气 pop | 档位从 N/1–5 变化时 |
| 换挡断油 | 发动机总线瞬时衰减 + RPM 回落 + 短暂牵引力切断 | 1–6 档升档时 |
| 自动降挡 | RPM 补油 + 高频进气瞬态 | 车速跌破降档阈值时 |
| 松油门 | 双段排气 crackle | 高于 3500 RPM 松开油门时 |
| 各档差异 | 档位共振峰 + 传动齿轮啸叫 | 随当前档位和车速连续变化 |
| 高转速 | 限转方波提示 | RPM > 7900 |
| 高速行驶 | 高通风噪 | 速度比例驱动 |
| 轮胎摩擦 | 带通胎噪 | 高速状态驱动 |

浏览器必须通过“启动引擎”按钮解锁 AudioContext；暂停、菜单和完成状态会将主音量平滑衰减。当前没有使用 ElevenLabs 生成文件，因为 `threejs_audio_asset.py probe` 返回 `ELEVENLABS_API_KEY=MISSING`；连续引擎声采用本地确定性合成，避免外部音频加载失败。

## 参考规范记录

- `threejs-gameplay-systems`：先定义驾驶核心循环，再调控制、镜头、音频反馈。
- `physics-tuning` + `timestep-and-ccd`：车辆纵向模拟改为固定 60 Hz、最多 5 次补帧，避免不同刷新率下加速不一致。
- `input-systems`：W/S/A/D 和方向键集中映射为 throttle / brake / steerLeft / steerRight 动作。
- `physics-engine-selection`：本原型采用变换驱动的街机车辆和赛道最近点约束，不引入刚体引擎。
- `game-feel`：速度响应、镜头跟随、音频同步和重启路径保持可调。
- `threejs-audio-generator` + `audio-workflows`：AudioContext 用户手势解锁、分组总线、状态驱动声音。
- `threejs-aaa-graphics-builder`：共享材质/资产、背景层次、活动视图优先；本轮不新增外部模型。
- `threejs-qa-release`：本轮先完成构建和静态检查；浏览器驾驶回归需在新路线加载后继续执行（重点检查起步、换挡、刹车、弯道和赛道边界）。

## 技术美术预算

- 目标：桌面 draw calls ≤ 300、三角形 ≤ 750k、纹理 ≤ 60；DPR 上限 1.5；阴影灯 1 个，1024 shadow map。
- 复用策略：Kenney 包仍从 `public/assets/kenney-selected` 共享加载；设施全部位于路肩外，柏油路面保持净空。
- 诊断入口：浏览器主世界中的 `window.__THREE_GAME_DIAGNOSTICS__()`，返回 renderer、赛道宽度/长度、当前档位和 RPM。

## 验证

- `node --check src/main.js`：通过。
- `npm run build`：通过，Vite 产物写入 `dist/`。
- `npm run preview -- --host 127.0.0.1 --port 4173`：通过（此前版本）。
- `npm run build`：本轮通过，Vite 产物写入 `dist/`。
- `node tools/track-geometry-check.mjs`：通过执行；Monza / Spa / Nürburgring 无中心线交叉，Silverstone 仍报告 1 处回叠交叉，局部道路使用净空收窄保险，尚未把该项标记为完全通过。
- 本轮新增物理与路线后，需在浏览器回归：确认 Monza 起步为 N→1、1→2 不早于 62 km/h；测试 S 键刹车/倒车、A/D 高速转向、`C` 两种第三人称视角和 `R` 重置。

## 可恢复备份

- `src/main.before-audio-pass.js`
- `src/style.before-audio-pass.css`
- `index.before-audio-pass.html`
