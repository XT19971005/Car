# 车辆审计报告

本次审计在车库逐台加载模型，并对轮组识别、前后轮判定、轮轴方向和轮心枢轴做了检查。轮心测试采用“临时旋转 0.41 弧度，再旋回”的可逆测试，最大漂移小于 0.025 m 才判定通过。滚动方向按车辆前进方向重新校正：+Z 前进、+X 轮轴时使用正角速度，不再额外取负；轮组名字包含 `front/rear` 或 `LF/RF/LR/RR` 时优先使用命名判定前后轮，避免模型转正后把后轮当成前轮。

| 车型 | 四轮独立节点 | 前后轮判定 | 轮心枢轴 | 方向盘节点 | 结论 |
| --- | ---: | --- | --- | --- | --- |
| KR-01 Clubsport | 4 | 2 前 / 2 后 | PASS | 无（使用座舱方向盘） | 可用 |
| KR-02 GT Cup | 4 | 2 前 / 2 后 | PASS | 无（使用座舱方向盘） | 可用 |
| KR-03 Future GT | 4 | 2 前 / 2 后 | PASS | 无（使用座舱方向盘） | 可用 |
| KR-04 Bigfoot | 4 | 2 前 / 2 后 | PASS | 无（使用座舱方向盘） | 可用 |
| KR-05 ZhiZun Monster | 4 | 2 前 / 2 后 | PASS | 无（使用座舱方向盘） | 可用 |
| KR-06 Car 011 | 0 | 无法判定 | 不可测试 | 无 | **需要重新拆分** |
| KR-07 Porsche 992 GT3 R | 4 | 2 前 / 2 后 | PASS | `STEER_HR_190` | 可用 |
| KR-08 McLaren MP4-17 | 0 | 无法判定 | 不可测试 | `Object_41` | **需要重新拆分** |
| KR-09 Cicada Retro | 4 | 2 前 / 2 后 | PASS | `steering_wheel` | 可用 |

## 需要重新拆分的模型

### `Car_011.glb`

当前只有一个合并网格（`Car_011 / Mesh`），没有可独立驱动的轮组和方向盘。请至少拆成以下节点：

```text
body
wheel_front_left
wheel_front_right
wheel_rear_left
wheel_rear_right
steering_wheel   （没有真实方向盘可省略）
```

### `McLaren MP4-17.glb`

当前车身/轮子是合并形式，虽然能找到一个方向盘网格，但四个轮组没有独立节点。请按相同命名拆分：

```text
body
wheel_front_left
wheel_front_right
wheel_rear_left
wheel_rear_right
steering_wheel
```

轮轴请沿模型局部 X 轴，轮组节点原点放在轮胎几何中心；前轮和后轮必须是四个独立父节点。这样游戏会自动识别轮轴、前轮转向和车轮滚动。

## 车内视角规则

有真实方向盘节点的车型（例如 Porsche 的 `STEER_HR_190`、McLaren 的 `Object_41`、Cicada 的 `steering_wheel`），车内镜头会读取方向盘几何中心，放到方向盘后方的驾驶员眼高，并把该节点克隆到隐藏车身之外的座舱层中显示和转动。没有真实方向盘节点的车型则使用统一的左舵驾驶位锚点和虚拟方向盘；不会回到车体正中。

## 本轮修正

- 清理车辆 Mod 中会脱离车身的 `AUX_LIGHT`、`RAIN_LIGHTS`、`LED_LIGHTS` 辅助灯节点；车辆自身的 `front_lamp_glass` / `rear_lamp_glass` 等车灯罩不受影响。
- 第三人称近景改为按车长自适应：普通跑车更近，大脚车和怪兽卡车略微后移，同时收回视线目标避免车尾被 HUD 完全裁掉。
- 车内视角仍保持固定驾驶员视线；鼠标拖动只影响第三人称环视，不会拉伸车内相机。
